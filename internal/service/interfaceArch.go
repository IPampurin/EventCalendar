// интерфейс архиватора - порт фонового процесса архивации (отдельная горутина по таймеру)
package service

import "context"

// Archiver - периодически переносит старые события в архив, реализация дергает сервис или хранилище
type Archiver interface {

	// Run - блокируется до отмены ctx, внутри - тикер и вызов логики архивации
	Run(ctx context.Context) error
}
